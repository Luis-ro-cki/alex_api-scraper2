<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <title>ALEX SCRAPER | PORTAL</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link href="https://fonts.googleapis.com/css2?family=Geist+Mono&display=swap" rel="stylesheet">
    <style>
        body { background-color: #0c0e0e; color: #00f0ff; font-family: 'Geist Mono', monospace; }
        .cyber-border { border: 1px solid rgba(0, 240, 255, 0.3); background: #111313; box-shadow: 0 0 15px rgba(0, 240, 255, 0.05); }
        .neon-text { text-shadow: 0 0 10px #00f0ff; }
        .status-pulse { animation: pulse 2s infinite; }
        @keyframes pulse { 0% { opacity: 1; } 50% { opacity: 0.3; } 100% { opacity: 1; } }
    </style>
</head>
<body class="p-4 md:p-10">
    <div class="max-w-6xl mx-auto">
        <header class="flex justify-between items-end border-b border-[#00f0ff]/40 pb-6 mb-10">
            <div>
                <h1 class="text-4xl font-black italic neon-text italic">ALEX_SCRAPER_API</h1>
                <p class="text-[10px] text-white/40 tracking-[0.3em] uppercase mt-2">Data Intelligence & Extraction System</p>
            </div>
            <div class="text-right">
                <p class="text-[10px] text-green-400 font-bold status-pulse tracking-widest">● SYSTEM_ONLINE</p>
                <p class="text-[9px] opacity-30 mb-2">HOST: RENDER_CLOUD_NODE_X</p>
                <div class="flex gap-2 justify-end">
                    <a href="/endpoints" class="text-[10px] uppercase tracking-widest border border-[#00f0ff]/40 px-3 py-1 rounded hover:bg-[#00f0ff]/10">Endpoints</a>
                    <a href="/dashboard" class="text-[10px] uppercase tracking-widest border border-[#00f0ff]/40 px-3 py-1 rounded hover:bg-[#00f0ff]/10">Mi cuenta</a>
                </div>
            </div>
        </header>

        <div class="grid grid-cols-1 md:grid-cols-3 gap-6 mb-10">
            <div class="cyber-border p-6 rounded">
                <p class="text-[10px] opacity-40 uppercase">Server Uptime</p>
                <h2 class="text-3xl font-bold mt-1"><%= uptime %></h2>
            </div>
            <div class="cyber-border p-6 rounded">
                <p class="text-[10px] opacity-40 uppercase">Latency Average</p>
                <h2 class="text-3xl font-bold mt-1"><%= avgLatency %>ms</h2>
            </div>
            <div class="cyber-border p-6 rounded">
                <p class="text-[10px] opacity-40 uppercase">Active Scrapers</p>
                <h2 class="text-3xl font-bold mt-1"><%= activeScrapers %></h2>
            </div>
        </div>

        <div class="cyber-border rounded-lg overflow-hidden mb-10">
            <div class="p-5 bg-[#00f0ff]/5 border-b border-[#00f0ff]/20">
                <h3 class="text-sm font-bold tracking-widest uppercase">API_KEY_CONTROL_PANEL</h3>
            </div>
            <div class="overflow-x-auto">
                <table class="w-full text-left text-xs">
                    <thead>
                        <tr class="opacity-40 border-b border-white/5">
                            <th class="p-5">IDENTIFIER</th>
                            <th>PLAN</th>
                            <th>USAGE_METRICS</th>
                            <th>STATUS</th>
                        </tr>
                    </thead>
                    <tbody>
                        <% Object.keys(keys).forEach(k => { %>
                        <% const acc = keys[k]; const limitReached = acc.used >= acc.limit; %>
                        <tr class="border-b border-white/5 hover:bg-[#00f0ff]/5 transition-all">
                            <td class="p-5 text-white"><%= k %></td>
                            <td><span class="bg-[#00f0ff]/10 text-[#00f0ff] px-2 py-1 rounded text-[10px]"><%= acc.plan %></span></td>
                            <td><%= acc.used %> / <%= acc.limit %></td>
                            <td class="<%= limitReached ? 'text-red-500' : 'text-green-500' %> italic font-bold">
                                <%= limitReached ? 'LIMIT_REACHED' : 'READY' %>
                            </td>
                        </tr>
                        <% }) %>
                    </tbody>
                </table>
            </div>
        </div>

        <div class="grid grid-cols-1 md:grid-cols-2 gap-4">
            <div class="text-[10px] opacity-30 uppercase tracking-widest">© 2024 ALEX INFRASTRUCTURE</div>
            <div class="text-[10px] text-right opacity-30 uppercase tracking-widest italic">Encrypted Secure Tunnel Enabled</div>
        </div>
    </div>
</body>
</html>
